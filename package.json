{
  "name": "bark-cf-worker",
  "version": "3.0.0",
  "description": "Bark push notification server on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250504.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.10.0"
  }
}
